// From the dataviz skill's validated reference palette (references/palette.md).
export const CHART_COLORS = {
  accent: '#2a78d6', // categorical slot 1 / sequential default
  muted: '#898781', // de-emphasis ink, context series
  baseline: '#c3c2b7', // reference lines / axis
  gridline: '#e1e0d9', // hairline gridlines
  divergingUnder: '#2a78d6', // consumed < target
  divergingOver: '#e34948', // consumed > target
} as const
